<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>update thumbnail_type set
	db_modified_utc =  timezone ('UTC', current_timestamp),
	width = case when :width is null then width else :width end,
	height = case when :height is null then height else :height end,
	name = case when :name is null then name else :name end,
	display_name = case when :displayName is null then display_name else :displayName end
 where id = :id</sql>
	<inputDefinition>nabu.web.cms.core.types.model.core.ThumbnailType</inputDefinition>
</jdbcService>