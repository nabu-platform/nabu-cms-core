<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>select l.name as language
from users u join nodes n on u.id = n.id
join languages l on n.language = l.id
where u.alias = :alias
and u.realm = :realm
and n.enabled = true
and n.verified is not null
and n.published is not null</sql>
</jdbcService>