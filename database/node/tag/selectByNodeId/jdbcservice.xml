<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<outputDefinition>nabu.web.cms.core.rest.types.tagOutput</outputDefinition>
	<sql>select
	nt.id,
	nt.name,
	tg.group
from 
	node_tag nt inner join tag t on nt.tag_id= t.id
			    inner join tag_group tg on t.group_id= tg.id
where 
	nt.node_id=:nodeId</sql>
</jdbcService>