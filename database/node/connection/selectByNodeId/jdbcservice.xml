<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<outputDefinition>nabu.web.cms.core.rest.types.nodeConnectionOutput</outputDefinition>
	<sql>select
	nc.id,
	nc.source_id,
	nc.target_id,
	ct.name,
	cs.name
from 
	node_connection nc inner join connection_type ct on nc.connectin_type_id= ct.id
					 inner join connection_status cs on nc.connection_status_id= cs.id
where
	nc.source_id= :nodeId or nc.target_id= :nodeId</sql>
</jdbcService>