<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<outputDefinition>nabu.web.cms.core.types.node.nodeAttachmentThumbnail</outputDefinition>
	<sql>select
	t.uri,
	t.size,
	t.content_type,
	tt.width,
	tt.height,
	tt.name
from node_attachment_thumbnail t join thumbnail_type tt on t.thumbnail_type_id = tt.id
where t.node_attachment_id = :nodeAttachmentId</sql>
</jdbcService>