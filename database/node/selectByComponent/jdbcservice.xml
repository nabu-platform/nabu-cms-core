<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<outputDefinition>nabu.web.cms.core.types.model.core.Node</outputDefinition>
	<sql>select
	id,
	created,
	modified,
	owner_id,
	published,
	verified,
	enabled,
	parent_id,
	version,
	priority,
	name,
	title,
	description,
	path,
	slug,
	language_id,
	component_id
from nodes
where component_id = :componentId
order by created, name, path desc</sql>
</jdbcService>