<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<outputDefinition>nabu.cms.core.types.model.core.Translation</outputDefinition>
	<sql>select
	t.id,
	t.created,
	t.modified,
	t.context,
	t.name,
	t.translation,
	t.language_id
from translations t join languages l on t.language_id = l.id
where l.name = :language
and t.context = :context
and t.name = :name</sql>
</jdbcService>