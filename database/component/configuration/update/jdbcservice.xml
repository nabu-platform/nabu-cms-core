<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<inputDefinition>nabu.web.cms.core.types.model.core.ComponentConfiguration</inputDefinition>
	<sql>update component_configuration set
	db_modified_utc =  timezone ('UTC', current_timestamp),
	key = case when :key is null then key else :key end,
	value = case when :value is null then value else :value end,
	frontend = case when :frontend is null then frontend else :frontend end,
	component_id = case when :componentId is null then component_id else :componentId end
 where id = :id</sql>
</jdbcService>