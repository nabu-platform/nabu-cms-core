<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<inputDefinition>nabu.web.cms.core.types.model.core.ComponentAction</inputDefinition>
	<sql>update component_action set
	db_modified_utc =  timezone ('UTC', current_timestamp),
	name = case when :name is null then name else :name end,
	display_name = case when :displayName is null then display_name else :displayName end,
	icon = case when :icon is null then icon else :icon end,
	mixin = case when :mixin is null then mixin else :mixin end,
	component_id = case when :componentId is null then component_id else :componentId end
 where id = :id</sql>
</jdbcService>