We never delete translations, so worst case you can reset them to empty to "remove" them, but the actual records will persist.
This allows us to do partial updates to translations without having to see all translations for an instance.