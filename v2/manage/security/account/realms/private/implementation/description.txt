This lists all the realms where the current user has a "user.manage" permission on.

Note that the user.manage permission is on a web application. Multiple web applications can be in the same realm, this can allow for a sort of "cross-application" management but that's just the way it is.