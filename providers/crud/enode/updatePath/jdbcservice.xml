<jdbcService xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance">
	<sql>update ~nodes set path = replace(path, :oldPath || '/', :newPath || '/') where path like :oldPath || '/%'</sql>
</jdbcService>