The create assumes that it gets a full data type (which it does at the time of writing).
This means that we if we restrict the create data type to omit some fields, we still get a full instance of the core data type.